package {{ group_id }}.rest.config;

import {{ group_id }}.rest.app.MockRestService;
import {{ group_id }}.rest.jackson.JacksonProvider;
import {{ group_id }}.rest.providers.HttpSuccessfulStatusHandler;

import com.google.common.collect.Sets;
import {{ group_id }}.rest.exception.mappers.WebApplicationExceptionMapper;

import javax.ws.rs.ApplicationPath;
import javax.ws.rs.core.Application;
import java.util.Set;

@ApplicationPath(JAXRSConfiguration.APPLICATION_PATH)
public class JAXRSConfiguration extends Application {

  //see src/main/webapp/WEB-INF/jboss-web.xml
  public static final String CONTEXT_ROOT = "{{ project_url }}";
  public static final String APPLICATION_PATH = "/rest";

  @Override
  public Set<Class<?>> getClasses() {
    return Sets.newHashSet(
      MockRestService.class,
      JacksonProvider.class,
      HttpSuccessfulStatusHandler.class,
      WebApplicationExceptionMapper.class
    );
  }
}
