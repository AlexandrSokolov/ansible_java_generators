package {{ group_id }}.commons.config;

import static {{ group_id }}.commons.config.TestPropertiesConfig.NOT_EXISTING_PROP_KEY;

public interface NotExistingStringConfig {

  @PropertyKey(NOT_EXISTING_PROP_KEY)
  String notExistingProperty();
}
